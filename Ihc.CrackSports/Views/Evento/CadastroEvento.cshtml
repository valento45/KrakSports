@using Ihc.CrackSports.Core.Objetos.Competicoes;
@model Evento

@{
    Layout = "_Layout";
}

<div class="container mt-5 ">
    <h3 class="text-semi-bold-orange mt-5 ms-5">Cadastro de evento</h3>
    <hr />

    <div class=" col-9 d-inline-flex mt-5 w-100">
        <form class="form-group form-control layout-orange-full radius-form" asp-action="CadastroEvento" asp-controller="Evento" method="post" style="border-color: #800000 ; padding: 5%;">

            <input asp-for="IdEvento" class="d-none"  />
            <input asp-for="IdClub1" class="d-none"  />
            <input asp-for="IdClub2" class="d-none" />
            

            <input class="d-none" />
            <input class="d-none" />
            <div class="text-center">
               @*  @if (string.IsNullOrEmpty(Model.DadosAluno.FotoAlunoBase64))
                {

                    <img class="mt-4" src="~/imgs/sem_imagem.png" style="width: 150px;  height: 150px;  border-radius: 90px; box-shadow: 0px 0px 1px 1px #00000055;" alt="imagem aluno" />
                }
                else
                {

                    var imagem = String.Format("data:image/png;base64,{0}", Convert.ToBase64String(Model.DadosAluno.GetBytesFotoAluno()));

                    <img class="mt-4" src="@imagem" style="width: 150px;  height: 150px;  border-radius: 90px; box-shadow: 0px 0px 1px 1px #00000055;" alt="imagem aluno" />
                }
                <br>   *@             

            </div>

            <!--Dados do evento-->
            <div id="pnlDadosEvento">
                <div class="form-outline mt-5 ag-field-content g-width-10">

                    <input asp-for="DataHoraEvento" type="datetime" id="txtDataHoraEvento" class="form-control ag-field-input ag-input-color-orange-full" minLenght="4" autocomplete="off" required />
                    <label id="txtDataHoraEventoLabel" class="form-label ag-field-label" for="txtDataHoraEvento">Data/Hora*</label>
                </div>             


                
            </div>
            <!--FIM Dados do evento-->
            

            <div class="mb-2 ms-5 mx-2 mt-5">
                <button id="btnSalvarEvento" class="btn  btn-block btn-confirm-radius" type="submit">Salvar</button>
                <a href="#" class=" btn-voltar mx-5 text-white" >Voltar</a>
            </div>
        </form>
    </div>

</div>